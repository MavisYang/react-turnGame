import React from 'react'
import HistoryIndex from "../components/turnGameCom/History";

const HistoryScope = ({ history }) => {
    return <HistoryIndex history={history} />
}

export default HistoryScope